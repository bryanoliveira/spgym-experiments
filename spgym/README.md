# Sliding Puzzles Gym Env

Place image folders at `./imgs` (e.g. `./imgs/dataset/*.jpg`). Then, instantiate the env with `sliding_puzzles.make(w=3, variation="image", image_folder="dataset", render_mode="human")`.